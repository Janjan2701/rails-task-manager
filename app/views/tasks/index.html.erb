<h1>List of Books to Read</h1>
<%= link_to 'Create a new book to read', new_task_path %>
  <% @tasks.each do |task| %>
  <h5><%= link_to task.title, task_path(task) %></h5>
  <p><%= task.details %></p>
  <p><%= task.completed %></p>
<% end  %>
